# Expo / RN
node_modules/
.expo/
dist/
web-build/
*.orig.*

# Native build output (owner-gated dev-client / store builds)
/ios/
/android/
*.ipa
*.apk
*.aab

# Env / secrets — never commit
.env
.env.*
!.env.example

# Metro / logs
*.log
npm-debug.*
yarn-error.*

# OS
.DS_Store

# Rail pass marker — never committed (per the rail protocol).
.claude-pass

# Next.js build output (web/)
.next/

# The rail report of record lives at the workdir root, but this repo is
# PUBLIC and REPORT.md is internal ops narrative. Kept local, never published;
# the canonical copy is ops_reports in the database, which is what LEAD reads.
REPORT.md
